recvの処理を正しくする。

debugコマンド解析

停止時に値を返す。

AWKクライアント

Vimクライアント

著作権表記

ヴァージョン

ヘルプ

define
	REG_PERL

実行ファイル名を変える

連絡先

 =======================================

svd
	bufferを保存するか聞かれる
	bufferの更新で、全消え状態が見えるので、ちらつく。

全般？
	fizzbuzz.sedで、正常終了時に制御がファイル末尾に飛ぶ。
		→ dコマンドによって、制御が末尾に飛んでいるだけであった。
			→ dじゃなくて、nに変えるべきか？
